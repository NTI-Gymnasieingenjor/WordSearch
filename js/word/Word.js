/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 *
 * @format
 * @flow strict-local
 */

export default class Word {
  constructor(word, id) {
    this.word = word;
    this.id = id;
  }
}
